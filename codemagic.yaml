workflows:
  dcm-analysis:
    name: DCM avoid-unrelated-type-assertions
    max_build_duration: 15
    instance_type: mac_mini_m1
    environment:
      # Sets the Flutter version to the version from fvm_config.json.
      flutter: fvm
      groups:
        - dcm_key
    scripts:
      - name: install dcm on codemagic
        script: |
          brew tap CQLabs/dcm
          brew install dcm
      - name: Install flutter packages
        script: flutter pub get
      - name: Run DCM Analysis with License key
        script: |
          mkdir -p dcm-results
          dcm analyze --ci-key=$DCM_KEY --email=$DCM_EMAIL_ID lib --json-path=$CM_BUILD_DIR/app/dcm-results/dcm.json
          test_report: app/dcm-results/dcm.json
